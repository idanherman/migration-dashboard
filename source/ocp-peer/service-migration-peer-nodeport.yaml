apiVersion: v1
kind: Service
metadata:
  name: migration-peer-nodeport
  namespace: migration-test-system  # Override via deploy script / NAMESPACE
spec:
  type: NodePort
  externalTrafficPolicy: Local
  selector:
    app: migration-peer
  ports:
    - port: 8082
      targetPort: 8082
      name: http
